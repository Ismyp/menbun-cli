{%- comment -%}
  Template für die Teamwear-Kalkulator Seite
  Verwendet eine dedizierte Section für den Konfigurator
{%- endcomment -%}

<section class="page-teamwear">
  <div class="section section--page-width">
    <div class="spacing-style">
      <header class="page-header">
        <h1 class="h1">Teamwear</h1>
        {%- if page.content != blank -%}
          <div class="rte">
            {{ page.content }}
          </div>
        {%- endif -%}
      </header>
    </div>
  </div>
</section>

{%- section 'teamwear-config' -%}